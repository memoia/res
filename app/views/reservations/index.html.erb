<h1>Listing reservations</h1>

<table>
  <thead>
    <tr>
      <th>Email</th>
      <th>Requested date</th>
      <th>Message</th>
      <th>Restaurant</th>
      <th>Confirmed</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @reservations.each do |reservation| %>
      <tr>
        <td><%= reservation.email %></td>
        <td><%= reservation.requested_date %></td>
        <td><%= reservation.message %></td>
        <td><%= reservation.restaurant_id %></td>
        <td><%= reservation.confirmed %></td>
        <td><%#= link_to 'Show', restaurant_reservation_path(restaurant_id) %></td>
        <td><%#= link_to 'Edit', edit_restaurant_reservation_path(restaurant_id) %></td>
        <td><%= link_to 'Destroy Reservation', reservation, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <td><%= link_to 'Destroy', reservations_path(reservation.id), method: :destroy, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%#= link_to 'New Reservation', new_restaurant_reservation_path %>